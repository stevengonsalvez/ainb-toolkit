# cline rules (deployed to `.clinerules/`)

Cline-specific rule store. `bootstrap.js` copies `cline-rulestore-rule.md` from this dir into the target project's `.clinerules/`. Generic guidance (Go, deps, env, MCP, etc.) is shared via `../general-rules/`.
