# Programme charter: godmode-test-slug (fixture)

## Outcome
Ship mdstat: a CLI reporting word/heading/link stats for markdown files.

## Model policy
BRAIN fable · BUILD opus+codex pair · TEST sonnet

## Verify doctrine
Library/CLI lane: unit + property tests; read artifacts, never blank-check.

## Termination
backlog_dry; budget 10M subagent tokens; no deadline.

## Dashboard
slug: godmode-test-slug

## LOOP PROTOCOL
Read charter + state every tick. State via Write/Edit tool only.
lease refresh every wake. Hooks own dashboard publish + sidecar sync.
