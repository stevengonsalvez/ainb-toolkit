# copilot rules (deployed to `.github/instructions/`)

GitHub Copilot rule store. `bootstrap.js` copies the rule files here into the target project's `.github/instructions/` (or merges into `AGENTS.md` if present). Generic rules come from `../general-rules/`.
