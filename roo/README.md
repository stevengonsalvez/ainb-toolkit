# roo rules (deployed to `.roo/rules/`)

Roo-specific rule store. `bootstrap.js` copies `roo-rulestore-rule.md` from this dir into the target project's `.roo/rules/`. Cross-tool generic rules live in `../general-rules/`.
