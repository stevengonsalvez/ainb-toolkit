## Design records

Frozen snapshots of plans and specs that informed reflect's evolution. Filenames are dated; content does not get edited after merge — if a design changes, write a new record that supersedes the old one.

These are reference material, not living documentation. For the current architecture, see [`../architecture.md`](../architecture.md).
